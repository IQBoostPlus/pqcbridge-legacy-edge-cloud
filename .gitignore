__pycache__/
*.pyc
.pytest_cache/
.venv/

# Runtime state (P04): device sequence counters and the cloud's
# persisted ML-KEM keypair must never be committed.
device_state/
cloud_state/

# Generic Python / tooling
venv/
env/
.coverage
htmlcov/
.vscode/
.idea/

# Secrets and credentials (never commit)
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer

# P08 BF-03A: real device/gateway credential files (never commit).
# The committed common/demo_*_keys.json files are DEMO-ONLY material.
device_keys.json
gateway_keys.json

# Logs and temporary data
*.log
state/
data/
tmp/
temp/
*.tmp

# P08 local tooling / temporary verification artifacts (never commit)
_p8python/
_p8deps/
_p8wheels/
_p8verify/
_p8*.zip
_p8get-pip.py
